[project]
name = "mcp-test"
version = "0.1.0"
description = "Projet pour étudier les serveurs MCP sur web"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "granian>=2.7.2",
    "mcp>=1.27.0",
    "starlette>=1.0.0",
]
